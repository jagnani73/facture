# Operator-only secrets, loaded by scripts/ and by no service.
# Kept out of packages/backend/.env on purpose: the venue's process must never
# hold the key that turns a maturity obligation into a payment.

# Maturity collection account
MATURITY_COLLECTION_PRIVATE_KEY!        # signs the scheduled payout by hand; account id lives in packages/backend/.env
